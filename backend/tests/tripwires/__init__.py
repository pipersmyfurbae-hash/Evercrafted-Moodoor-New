"""Drift tripwires.

CLAUDE.md, "Preventing Drift" item 5: a rule in a markdown file can be
silently violated over time; a rule enforced by an automated test cannot.
These run on every sprint's output, not just the sprint that wrote them.
"""
